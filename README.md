# ROS 2 Jazzy local simulation environment

The reusable project lives in:

```text
ros2-jazzy-sim/
```

It is organized as a self-contained subdirectory so it can be copied or committed into a larger GitHub repository later.
